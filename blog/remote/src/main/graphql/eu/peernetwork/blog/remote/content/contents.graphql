query Getallposts(
    $filter: [PostFilterType!],
    $sort: PostSortType,
    $tag: String,
    $title: String,
    $userId: ID,
    $postId: ID,
    $offset: Int,
    $limit: Int
) {
    listPosts(
        filterBy: $filter,
        IgnorList: YES,
        sortBy: $sort,
        tag: $tag,
        title: $title,
        userid: $userId,
        postid: $postId,
        limit: $limit,
        offset: $offset
    ) {
        status
        counter
        ResponseCode
        affectedRows {
            id
            title
            mediadescription
            isliked
            isviewed
            isdisliked
            amountlikes
            amountdislikes
            amountviews
            amountcomments
            media
            cover
            contenttype
            createdat
            user {
                id
                username
                slug
                img
                isfollowed
                isfollowing
            }
        }
    }
}
