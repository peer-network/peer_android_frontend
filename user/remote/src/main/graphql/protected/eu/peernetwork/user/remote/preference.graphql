query Preference {
    getUserInfo {
        status
        ResponseCode
        affectedRows {
            userPreferences {
                contentFilteringSeverityLevel
            }
        }
    }
}
