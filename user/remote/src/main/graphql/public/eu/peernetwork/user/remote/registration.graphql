mutation Register($email: String!, $password: String!, $username: String!, $referral: ID) {
    register(input: { email: $email, password: $password, username: $username, referralUuid: $referral }) {
        status
        ResponseCode
        userid
        meta {
            status
            RequestId
            ResponseCode
        }
    }
}
