query ListFollowRelations(
    $userid: ID!,
    $contentFilterBy: ContentFilterType,
    $offset: Int!,
    $limit: Int!
) {
    listFollowRelations(
        userid: $userid,
        contentFilterBy: $contentFilterBy,
        offset: $offset,
        limit: $limit
    ) {
        status
        counter
        ResponseCode
        affectedRows {
            followers {
                id
                username
                slug
                img
                isfollowed
                isfollowing
                isHiddenForUsers
                visibilityStatus
            }
        }
    }
}
