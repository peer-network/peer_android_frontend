mutation UpdateChatInformations($chatid: ID!, $name: String, $image: String) {
    updateChatInformations(input: { chatid: $chatid, name: $name, image: $image }) {
        status
        ResponseCode
        affectedRows {
            chatid
        }
    }
}
